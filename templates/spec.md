# Spec: <title>

## Problem Statement

The problem, from the user's perspective.

## Solution

The solution, from the user's perspective.

## Test seams

The seams at which this feature will be tested. Prefer existing seams. Use
the highest seam possible. Fewest new seams — ideal is one. If a seam choice
is a live trade-off, it was parked as a decision card before this spec was
finalized (reference it).

## User Stories

A LONG, numbered list. Extensive. Each:

1. As an <actor>, I want <feature>, so that <benefit>.

## Implementation Decisions

- Modules built/modified, their interface changes
- Technical clarifications, architectural decisions
- Schema changes, API contracts, key interactions

No file paths, no code snippets (they go stale). Exception: a
prototype-produced shape (state machine, reducer, schema, type form) may be
inlined within the relevant decision, trimmed to decision-rich parts, noting
it came from a prototype.

## Testing Decisions

- What makes a good test here (external behaviour only, not implementation)
- Which modules get tests
- Prior art: similar existing tests to follow

## Out of Scope

Explicitly out. Guard against scope creep from these.

## Further Notes

Sources: transcript dates/IDs, decision card references.
