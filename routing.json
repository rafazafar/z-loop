{
  "$comment": "Single source of truth for local loop configuration. Alias models are provider/model ids resolved by your GLOBAL opencode config. Variants set provider-specific reasoning effort. All scripts read this file via run/common.sh.",
  "project": {
    "$comment": "The GitHub repository is derived from this checkout's origin remote.",
    "repo_path": "/Users/zafar/dev/kokolog-monitor"
  },
  "aliases": {
    "code-max": {
      "model": "openai/gpt-5.6-luna",
      "variant": "max"
    },
    "code-mid": {
      "model": "openai/gpt-5.6-sol",
      "variant": "high"
    },
    "doc-xhigh": {
      "model": "zai-coding-plan/glm-5.3",
      "variant": "max"
    },
    "doc-fast": {
      "model": "zai-coding-plan/glm-5.3",
      "variant": "max"
    }
  },
  "roles": {
    "implementer": {
      "model": "code-max"
    },
    "reviewer": {
      "model": "code-mid"
    },
    "ticketer": {
      "model": "code-mid"
    },
    "distiller": {
      "model": "doc-xhigh"
    },
    "gardener": {
      "model": "code-mid"
    },
    "decision-desk": {
      "model": "code-mid"
    }
  },
  "rules": {
    "$comment_rules": "Policy executed by run/loop-tick in the fix-retry and reaper paths. Static policy, dynamic behavior.",
    "retry_tier_up": {
      "$comment": "Fires only when the implementer is tuned DOWN to code-mid (cost mode): past N failures it escalates to code-max. With the default SOTA pin (code-max) it is correctly inert.",
      "after_failures": 2,
      "from": "code-mid",
      "to": "code-max"
    },
    "max_verify_rounds": 3,
    "max_impl_attempts": 3,
    "diff_budget": {
      "$comment": "Default review threshold for hand-written semantic changes. Generated scaffolding, lockfiles, and mechanical output can exceed it when the ticket remains one coherent outcome.",
      "max_files": 12,
      "max_insertions": 800,
      "max_deletions": 400
    },
    "session_timeout_sec": 3600,
    "clone_reap_days": 7
  },
  "github": {
    "$comment": "ready-for-worker labels the frontier query. loop-integration marks a parent issue that is a real integration task; the frontier ignores labeled parents without it. branch_prefix creates stable issue branches as <prefix>-<issue>/<title-slug>. base_branch is the required fresh remote branch and PR base. Create labels once: gh label create ready-for-worker; gh label create loop-integration",
    "frontier_label": "ready-for-worker",
    "integration_label": "loop-integration",
    "branch_prefix": "issue",
    "base_branch": "main"
  }
}
