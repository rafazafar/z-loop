# config.env — copied to config.env (gitignored). Overrides only; no secrets
# needed: opencode resolves providers (9router, openai, zai coding plan) from
# your GLOBAL opencode config.
# Sourced by run/common.sh on every invocation.

# client repo location (default shown)
#KOKOLOG_REPO=/Users/zafar/dev/kokolog-monitor
