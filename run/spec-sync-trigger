#!/bin/bash
# Run one committed meeting transcript. The runtime owns the claim lock.
set -euo pipefail
LOOP_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec python3 "$LOOP_ROOT/run/spec_sync/queue.py" "$@"
